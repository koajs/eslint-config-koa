language: node_js
node_js: node
env:
  - STANDARD_VERSION=5
  - STANDARD_VERSION=6
  - STANDARD_VERSION=7
  - STANDARD_VERSION=8
  - STANDARD_VERSION=9
  - STANDARD_VERSION=10
  - STANDARD_VERSION=11
cache:
  directories:
    - node_modules
before_install:
  - npm install --global install-peerdeps
  - echo "eslint-config-standard@${STANDARD_VERSION}" | xargs install-peerdeps
